% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allClass-S4.R, R/allProbability-S4.R,
%   R/allExpectation-S4.R
\docType{class}
\name{CalibrationParam-class}
\alias{CalibrationParam-class}
\alias{probability_distribution}
\alias{expected_value}
\alias{expected_pcds_loss}
\alias{expected_cdo_loss}
\alias{expected_pcds_equation}
\alias{expected_cdo_equation}
\alias{expected_value,CalibrationParam-method}
\alias{expected_pcds_loss,CalibrationParam-method}
\alias{expected_cdo_loss,CalibrationParam-method}
\alias{expected_pcds_equation,CalibrationParam-method}
\alias{expected_cdo_equation,CalibrationParam-method}
\title{Virtual super-class for calibration parameters}
\usage{
probability_distribution(object, times)

expected_value(object, times, g, ...)

expected_pcds_loss(object, times, recovery_rate, ...)

expected_cdo_loss(object, times, recovery_rate, lower, upper, ...)

expected_pcds_equation(
  object,
  times,
  discount_factors,
  recovery_rate,
  coupon,
  upfront,
  ...
)

expected_cdo_equation(
  object,
  times,
  discount_factors,
  recovery_rate,
  lower,
  upper,
  coupon,
  upfront,
  ...
)

\S4method{expected_value}{CalibrationParam}(object, times, g, ...)

\S4method{expected_pcds_loss}{CalibrationParam}(object, times, recovery_rate, ...)

\S4method{expected_cdo_loss}{CalibrationParam}(object, times, recovery_rate, lower, upper, ...)

\S4method{expected_pcds_equation}{CalibrationParam}(
  object,
  times,
  discount_factors,
  recovery_rate,
  coupon,
  upfront,
  ...
)

\S4method{expected_cdo_equation}{CalibrationParam}(
  object,
  times,
  discount_factors,
  recovery_rate,
  lower,
  upper,
  coupon,
  upfront,
  ...
)
}
\arguments{
\item{object}{The calibration parameter object}

\item{times}{Point-in-time}

\item{g}{Transformation function.}

\item{...}{Pass-through parameter}

\item{recovery_rate}{The recovery rate of the portfolio CDS/CDO.}

\item{lower}{Lower attachment point of the CDO tranche.}

\item{upper}{Upper attachment point of the CDO tranche.}

\item{discount_factors}{Discount factors for \code{times}}

\item{coupon}{Running coupon}

\item{upfront}{Upfront payment}
}
\description{
A virtual superclass for all calibration parameters for multivariate
(portfolio) credit models.
}
\details{
It is assumed that the expected value of the credit derivative
payment streams can be represented as a function of \emph{expected losses} and
other non-model dependent quantities.
\emph{Expected losses} are the expected values of the average default counting
process \eqn{L} under a transformation \eqn{g} on times \eqn{t_1, \ldots, t_n},
i.e. \eqn{E[g(L_{t_1})], \ldots, E[g(L_{t_n})]}.

A \code{CalibrationParam} object is supposed to be used to abstract the calculation
of these \emph{expected losses} in functions which calculate the fair value of the
option with the asset pricing theorem.

\code{\link[=expected_value]{expected_value()}} calculates for a function \eqn{g} and the average default
counting process \eqn{L} the expectation
\deqn{
  \mathbb{E}[g(L_t)] .
}
For a portfolio CDS choose \eqn{g(x) = (1 - R) x} and for a CDO tranche with
attachment points \eqn{l < u} and choose
\eqn{g(x) = min{\{ \max{\{ (1 - R) x - l, 0 \}}, u - l \}}}, where \eqn{R} is
the recovery rate.
}
\section{Functions}{
\itemize{
\item \code{probability_distribution}: returns the probability vector for the average default count process \eqn{L}.

\item \code{expected_value}: returns the expected value for the average default count process \eqn{L} at
a specific time-point.

\item \code{expected_pcds_loss}: returns the expected portfolio CDS loss for a specific time-point.

\item \code{expected_cdo_loss}: returns the expected CDO loss for a specific time-point.

\item \code{expected_pcds_equation}: returns the expected portfolio CDS fair-value equation.

\item \code{expected_cdo_equation}: returns the expected CDO fair-value equation.
}}

\section{Slots}{

\describe{
\item{\code{dim}}{The dimension (number of portfolio items)}
}}

\examples{
expected_value(CuadrasAugeExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3,
  function(x) pmin(pmax(0.6 * x - 0.1, 0), 0.2))
expected_value(AlphaStableExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3,
  function(x) pmin(pmax(0.6 * x - 0.1, 0), 0.2))
expected_value(PoissonExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3,
  function(x) pmin(pmax(0.6 * x - 0.1, 0), 0.2))
expected_value(ExponentialExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3,
  function(x) pmin(pmax(0.6 * x - 0.1, 0), 0.2))

expected_pcds_loss(CuadrasAugeExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3, 0.4)
expected_pcds_loss(AlphaStableExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3, 0.4)
expected_pcds_loss(PoissonExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3, 0.4)
expected_pcds_loss(ExponentialExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3, 0.4)

expected_cdo_loss(CuadrasAugeExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3, 0.4, 0.1, 0.2)
expected_cdo_loss(AlphaStableExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3, 0.4, 0.1, 0.2)
expected_cdo_loss(PoissonExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3, 0.4, 0.1, 0.2)
expected_cdo_loss(ExponentialExtMO2FParam(
  dim = 50, lambda = 0.05, rho = 0.4), 0.3, 0.4, 0.1, 0.2)

expected_pcds_equation(
  ExtGaussian2FParam(dim = 75, lambda = 0.05, rho = 0.6),
  times = seq(0.25, 5, by = 0.25), discount_factors = rep(1, 20),
  recovery_rate = 0.4, coupon = 0.08, upfront = 0
)
expected_pcds_equation(
  ExtGaussian2FParam(dim = 75, lambda = 0.05, rho = 0.6),
  times = seq(0.25, 5, by = 0.25), discount_factors = rep(1, 20),
  recovery_rate = rep(0.4, 4), coupon = rep(0.08, 4), upfront = rep(0, 4)
)

expected_cdo_equation(
  ExtGaussian2FParam(dim = 75, lambda = 0.05, rho = 0.6),
  times = seq(0.25, 5, by = 0.25), discount_factors = rep(1, 20),
  recovery_rate = 0.4, lower = c(0, 0.1, 0.2, 0.35),
  upper = c(0.1, 0.2, 0.35, 1), coupon = 0.08, upfront = 0
)

}
\seealso{
\linkS4class{ExMarkovParam} \linkS4class{ExMOParam} \linkS4class{ExtMOParam}
\linkS4class{CuadrasAugeExtMO2FParam} \linkS4class{AlphaStableExtMO2FParam}
\linkS4class{PoissonExtMO2FParam} \linkS4class{ExponentialExtMO2FParam}
}
